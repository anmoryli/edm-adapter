# EDM-Adapter 实验报告

## 1. 数据集统计

### 1.1 数据来源

- 主数据集: MTG-Jamendo / FMA
- 筛选标签: electronic, techno, house, ambient, trap, drum and bass, trance, dubstep

### 1.2 数据规模

| 项目 | 数量 |
|------|------|
| 原始轨道数 | (待填充) |
| 筛选后轨道数 | (待填充) |
| 切片后片段数 | (待填充) |
| 通过质量过滤 | (待填充) |
| 最终训练集 | (待填充) |
| 验证集 | (待填充) |
| 测试集 | (待填充) |

### 1.3 风格分布

(运行 `python scripts/11_make_report_assets.py` 后填充)

### 1.4 BPM 分布

(运行 `python scripts/11_make_report_assets.py` 后填充)

## 2. 训练配置

### 2.1 硬件环境

- GPU: (待填充)
- VRAM: (待填充)

### 2.2 Stable Audio Open 微调配置

| 参数 | 值 |
|------|-----|
| 模型 | stabilityai/stable-audio-open-1.0 |
| 学习率 | 1e-5 |
| 批大小 | 4 |
| 梯度累积 | 2 |
| 训练轮数 | 3 |
| FP16 | Yes |
| 片段长度 | 10 秒 |

### 2.3 MusicGen 微调配置 (备选)

| 参数 | 值 |
|------|-----|
| 模型 | facebook/musicgen-small |
| 学习率 | 1e-4 |
| 批大小 | 4 |
| 训练轮数 | 3 |

## 3. Baseline 结果

使用预训练模型 (未微调) 在固定 prompts 上生成的结果。

(运行 `python scripts/07_baseline_generate.py` 后填充)

## 4. 微调后结果

使用微调后模型在相同 prompts 上生成的结果。

(运行 `python scripts/09_generate_after_finetune.py` 后填充)

## 5. 微调前后对比

### 5.1 自动指标对比

(运行 `python scripts/10_evaluate.py` 后填充)

| 指标 | Baseline | Finetuned | 改善 |
|------|----------|-----------|------|
| BPM Error (mean) | | | |
| RMS Energy | | | |
| Low Freq Ratio | | | |
| Onset Density | | | |
| Loop Similarity | | | |
| Silence Ratio | | | |

### 5.2 对比图表

(运行 `python scripts/11_make_report_assets.py` 后生成)

## 6. 自动指标分析

### 6.1 BPM 准确性

(分析微调后 BPM 误差是否降低)

### 6.2 低频表现

(分析微调后低频能量比例是否更接近真实电子音乐)

### 6.3 Loop 可用性

(分析首尾相似度是否提升)

## 7. 人工听感分析

### 7.1 评估方法

5 位评估者，对 6 组 prompt 的 baseline 和 finetuned 结果进行盲听评分。

### 7.2 评分维度

1. 风格匹配度 (1-5)
2. 鼓点稳定性 (1-5)
3. Bass 表现 (1-5)
4. 电子音乐质感 (1-5)
5. Loop 可用性 (1-5)

### 7.3 评分结果

(待填充)

## 8. 失败样例分析

(分析生成质量较差的样例，总结原因)

## 9. 总结与改进方向

### 9.1 总结

- (待填充)

### 9.2 改进方向

1. 扩大训练数据规模 (3000-10000 片段)
2. 增加更多电子音乐子风格
3. 探索 LoRA 等参数高效微调方法
4. 增加 BPM 和 Key 的精确控制
5. 支持更长时长 (20s/30s) 的生成
6. 训练电子音乐风格分类器作为辅助评估
